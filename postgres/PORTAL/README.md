# PORTAL database

Database: `PORTAL`

Purpose: platform metadata, portal settings, admin data, tool registry, permissions and platform-wide configuration.

Schemas:

- `core` - platform core, shared metadata, registry, permissions and audit.
- `admin` - admin panel objects and workflow around tool management.
- `portal` - user-facing portal settings, themes, navigation and layout preferences.
